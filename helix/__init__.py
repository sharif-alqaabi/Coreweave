"""Helix — shared plumbing for the Scout → Critic → Evolve loop.

Agents live in `agents/`, the living toolkit in `kit/`, and this package
holds what they all share: the lead schema, ledger IO, kit loading, and
telemetry. Keep science out of here.
"""

__version__ = "0.0.1"
