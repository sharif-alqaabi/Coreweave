# Helix as state graphs

Generated by `python -m helix.orchestrate mermaid`. Nodes call the same functions as `loop.py` and `helix/product.py`; the graph adds checkpointing (resume from the last completed node), fan-out for the architects, and an approval interrupt before a rulebook is promoted.

## Product pipeline

```mermaid
---
config:
  flowchart:
    curve: linear
---
graph TD;
	__start__([<p>__start__</p>]):::first
	start(start)
	summarize(summarize)
	propose(propose)
	attach(attach)
	judge(judge)
	verify(verify)
	link(link)
	__end__([<p>__end__</p>]):::last
	__start__ --> start;
	attach --> judge;
	judge --> verify;
	propose --> attach;
	start -.-> summarize;
	start -.-> verify;
	summarize --> propose;
	verify -.-> __end__;
	verify -.-> link;
	link --> __end__;
	classDef default fill:#f2f0ff,line-height:1.2
	classDef first fill-opacity:0
	classDef last fill:#bfb6fc
```

## Training round

```mermaid
---
config:
  flowchart:
    curve: linear
---
graph TD;
	__start__([<p>__start__</p>]):::first
	judge(judge)
	architect_deepseek(architect_deepseek)
	architect_qwen3(architect_qwen3)
	architect_aria(architect_aria)
	tournament(tournament)
	approval(approval)
	promote(promote)
	__end__([<p>__end__</p>]):::last
	__start__ --> judge;
	approval --> promote;
	architect_aria --> tournament;
	architect_deepseek --> tournament;
	architect_qwen3 --> tournament;
	judge --> architect_aria;
	judge --> architect_deepseek;
	judge --> architect_qwen3;
	tournament --> approval;
	promote --> __end__;
	classDef default fill:#f2f0ff,line-height:1.2
	classDef first fill-opacity:0
	classDef last fill:#bfb6fc
```
