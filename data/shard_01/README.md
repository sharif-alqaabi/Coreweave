# shard_01

Drop one small corpus here for V0: a handful of papers (as .txt/.md) and one
tabular dataset (CSV/TSV with a contrast: log2FC, p, q columns).

Keep it small. A loop that cannot finish in minutes will not evolve.
