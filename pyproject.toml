[project]
name = "helix"
version = "0.0.1"
description = "Self-improving multi-agent loop: Scout → Critique → Evolve → Repeat"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
